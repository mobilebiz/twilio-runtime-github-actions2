{
  "name": "twilio-runtime-github-actions2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo 'Sorry, we have not test code.'",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^2.8.0"
  },
  "engines": {
    "node": ">=8.0.0 <11.0.0"
  }
}
